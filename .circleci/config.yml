version: 2.1
jobs:
  build:
    machine: true
    steps:
      - checkout
      - run: docker build -t rckkmv/ci-notify:latest .
      - run: docker login -u rckkmv -p $DOCKERHUB_AUTH
      - run: docker push rckkmv/ci-notify:latest
