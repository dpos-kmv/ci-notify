version: 2.1
jobs:
  build:
    machine: true
    steps:
      - checkout
      - run: |
          docker build -t rckkmv/ci-notify:latest .
          docker login -u $DOCKER_USER -p $DOCKERHUB_AUTH
          docker push rckkmv/ci-notify:latest
